/*const indentIt = (nb: number) => {
  let indent = '';
  for (let i = 0; i < nb; i += 1) {
    indent += '  ';
  }
  return indent;
};

const browse = (obj: any, indent: number) => {
  let content = '';
  if (obj instanceof Array) {
    obj.forEach((e: any) => {
      content += browse(e, indent + 1);
    });
  } else if (typeof obj === 'object' && obj !== null) {
    for (const key in obj) {
      if(Object.keys())
    }
  } else {
  }
  return content;
};

const toYaml = (json: any) => {
  const yaml = '';
  for (const key in json) {
  }
};
*/
